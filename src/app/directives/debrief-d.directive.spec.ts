import { DebriefDDirective } from './debrief-d.directive';

describe('DebriefDDirective', () => {
  it('should create an instance', () => {
    const directive = new DebriefDDirective();
    expect(directive).toBeTruthy();
  });
});
